[package]
name = "invocation"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Pure translation of command-line argument tokens into a validated invocation request."

[lib]
name = "invocation"
path = "src/lib.rs"

[dependencies]
# Aliased so references are not confused with the standard library `core`.
foundation = { package = "core", path = "../core" }
