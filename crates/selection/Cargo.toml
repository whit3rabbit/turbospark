[package]
name = "mrefrust-selection"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
description = "Candidate selection: shaping configuration and the deterministic-or-randomized draw."

[lib]
name = "mrefrust_selection"
path = "src/lib.rs"

[dependencies]
# Aliased so references are not confused with the standard library `core`.
foundation = { package = "mrefrust-core", path = "../core" }
