//! NOT YET IMPLEMENTED. Part of qwen4_exp Phase 3's remaining work; see
//! `mod.rs`'s pseudocode header for what this module owns. Nothing in
//! this crate calls into it yet -- `RealQwen4State` (`state.rs`) is not
//! wired into `RealForwardRunner` or `real_forward_open.rs`.
