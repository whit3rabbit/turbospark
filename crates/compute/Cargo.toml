[package]
name = "turbospark-compute"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Destination-selected compute strategy for the model forward pass."

[lib]
name = "turbospark_compute"
path = "src/lib.rs"

[dependencies]
# Aliased so references are not confused with the standard library `core`.
foundation = { package = "turbospark-core", path = "../core", version = "0.1.0" }
